name: backend
description: Familiarise Backend - Dart Frog API with Better Auth compatible endpoints
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  bcrypt: ^1.1.3
  dart_frog: ^1.2.0
  dart_jsonwebtoken: ^2.14.1
  dotenv: ^4.2.0
  postgres: ^3.4.5
  prisma_flutter_connector:
    path: /Users/kaustavghosh/Desktop/prisma-flutter-connector
  uuid: ^4.5.1

dev_dependencies:
  dart_frog_lint: ^0.1.0
  mocktail: ^1.0.3
  test: ^1.25.5
